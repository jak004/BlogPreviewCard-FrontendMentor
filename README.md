# Blog Preview Card

A responsive implementation of the Frontend Mentor Blog Preview Card challenge, built from scratch with semantic HTML and CSS.

## Tech

- HTML5
- CSS3
- Local Figtree font
- Responsive layout

## Run locally

Open `index.html` in a browser, or serve the folder with any static web server.

The implementation does not use a framework or JavaScript.
